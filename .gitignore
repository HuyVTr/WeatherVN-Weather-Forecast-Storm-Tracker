# Python
__pycache__/
*.pyc

# Virtual environment
venv/
env/
ENV/
.env
.venv

# Project specific
project_data/

# Old services
services/realtime_weather_old/
*.txt
!requirements.txt

# 1. Bỏ qua file dữ liệu huấn luyện vì quá nặng (>200MB)
FINAL_TRAINING_DATASET_ENHANCED.csv

# 2. Bỏ qua toàn bộ file kết quả dự báo sinh ra (để repo sạch sẽ)
project_data/processed_output/*.csv

# 3. Đảm bảo GIỮ LẠI file mô phỏng bão Rai (vì nó nhẹ và cần để chạy mục 5)
!services/storm_prediction_service/simulation_data/actual_path.csv
# 4. Agent Tools (Dùng lệnh assume-unchanged thay thế để dùng được phím @)
test_write.txt
.vscode/
